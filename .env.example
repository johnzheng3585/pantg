# Server-side proxy target. Use the Cloudreve root URL or the full /api/v4 URL.
CLOUDREVE_API_BASE=http://localhost:5212/api/v4

# Browser-side API base. Keep /api/cloudreve to use the Next.js proxy above.
NEXT_PUBLIC_CLOUDREVE_API_BASE=/api/cloudreve
